# Fictional sample data

`movies.csv` contains three invented films, people, and companies. It was written for the offline demo; it is not an IMDb export and makes no claim about real productions.

The list fields use JSON arrays. Run `python -m imdb_extractor.demo --output-dir data/demo` from the project root to create a CSV export and a two-page PDF report without network access or credentials.
